import type { FC } from 'react';

export const PrivacyPolicyContent: FC = () => {
  return (
    <div className={'flex justify-center items-center text-primary font-semibold text-xl'}>
      Em contrução
    </div>
  );
};
